	{# see SonataAdminBundle:CRUD:list__batch.html.twig for the current default template #}

	{#{% extends getTemplate('../../../../../vendor/sonata-project/admin-bundle/Resources/views/CRUD/base_list_field.html.twig') %}#}
    {% extends getTemplate('SonataAdminBundle:CRUD:base_list_field.html.twig') %}

	{% block field %}
        <input type="checkbox" name="idx[]" value="{{ admin.id(object) }}" />

        {# the new radio button #}
        <input type="radio" name="targetId" value="{{ admin.id(object) }}" />
    {% endblock %}